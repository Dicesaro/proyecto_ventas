<%= form_with(model: pedido) do |form| %>
  <% if pedido.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibieron guardar este pedido:</h2>

      <ul>
        <% pedido.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Campo para Código del Pedido -->
  <div>
    <%= form.label :codigo, "Código del Pedido", style: "display: block" %>
    <%= form.text_field :codigo, placeholder: "Ingresa el código del pedido" %>
  </div>

  <!-- Selector de Cliente -->
  <div>
    <%= form.label :cliente_id, "Cliente", style: "display: block" %>
    <%= form.collection_select :cliente_id, Cliente.all, :id, :nombre, prompt: "Selecciona un cliente" %>
  </div>

  <!-- Selector de Estado -->
  <div>
    <%= form.label :estado, "Estado del Pedido", style: "display: block" %>
    <%= form.select :estado, [["Pendiente", "pendiente"], ["Entregado", "entregado"]], prompt: "Selecciona un estado" %>
  </div>

  <!-- Campo de Fecha -->
  <div>
    <%= form.label :fecha, "Fecha", style: "display: block" %>
    <%= form.datetime_field :fecha %>
  </div>

  <!-- Botón de Enviar -->
  <div>
    <%= form.submit "Guardar Pedido", class: "btn btn-save" %>
  </div>
<% end %>
